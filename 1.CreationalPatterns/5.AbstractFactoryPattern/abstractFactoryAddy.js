// aims to encapsulate a group of individual factories with a common goal.
// separates the details of implementation of a set of objects from their general usage

// Uses: if a system must be independent from the way the objects it creates are generated
// or it needs to work with multiple types of objects
const abstractVehicleFactory = (function() {
  
})();
